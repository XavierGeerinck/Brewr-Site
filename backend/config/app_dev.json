{
    "database": {
        "adapters": {
            "brewrDB": "sails-postgresql"
        },
        "connections": {
            "simple": {
                "adapter": "brewrDB",
                "host": "localhost",
                "port": 5432,
                "user": "postgres",
                "password": "root",
                "database": "brewr"
            }
        }
    },
    "server": {
        "ip": "0.0.0.0",
        "port": 8000,
        "cors_client_origins": [ "*" ],
        "cors_headers": [ "Authorization", "Content-Type", "If-None-Match", "Bearer", "x-http-method-override" ],
        "cors_methods": [ "GET", "HEAD", "POST", "PUT", "DELETE", "OPTIONS" ],
        "cors_credentials": true
    }
}
